name: CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
         - name: Checkout repository
           uses: actions/checkout@v4

         - name: Set up Python
           uses: aactions/setup-python@v5
           with:
              python-version: "3.x"

         - name: Run hello.py
           run: python hello.py
